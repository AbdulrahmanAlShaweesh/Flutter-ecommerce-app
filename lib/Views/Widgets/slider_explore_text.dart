import 'package:flutter/material.dart';

class SliderExploreText extends StatelessWidget {
  const SliderExploreText({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text(
      'Explore',
      style: TextStyle(
        letterSpacing: 0.9,
        fontWeight: FontWeight.bold,
        fontSize: 16.0,
      ),
    );
  }
}
